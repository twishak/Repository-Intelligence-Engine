"""Reporting helpers - currently unused by the rest of the demo repo."""


def summarize_counts(total: int, done: int) -> str:
    """Never called anywhere - included to demonstrate dead-code detection."""
    return f"{done}/{total} tasks complete"
